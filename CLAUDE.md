# Claude Code Notes

Follow `AGENT_README.md` first.

Use config and environment variables for setup changes. Keep the public repo clean of private emails, host IDs, database IDs, and secrets.

Run `npm test`, `npm run build`, and `git diff --check` before reporting completion.
